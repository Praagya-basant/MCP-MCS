# Frontend (Vite) — safe to expose in the browser bundle
VITE_SUPABASE_URL=https://ztxqksvexjonqmfyjijf.supabase.co
VITE_SUPABASE_ANON_KEY=

# Web push VAPID public key (the matching private key is a Supabase Edge
# Function secret only — see CLAUDE.md — never put it here). Public keys
# are meant to ship in the browser bundle, this is not a leak.
VITE_VAPID_PUBLIC_KEY=BPl7xcYsVbNvSGeseJHew92U1cKce3RGgZ8KoQ3uucRUza1ET7VknzJoqID2FaOKNUyDdovOnIx6A6QSyPCO09g

# NOTE: Resend must NEVER be called from the frontend, so VITE_RESEND_API_KEY
# is intentionally unused by any src/ code. Set the real key as a Supabase Edge
# Function secret instead (no VITE_ prefix, never bundled to the browser):
#   supabase secrets set RESEND_API_KEY=your_key
# This line is kept only so the var name matches what was requested.
VITE_RESEND_API_KEY=
